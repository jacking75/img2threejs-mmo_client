# 상세 구현 계획 — 애니메 필드 RPG 프로토타입

## 1. 목표와 범위

스타일화된 판타지 3인칭 RPG의 로컬·오프라인·데스크톱 브라우저 세로 기능 묶음을
구현한다. 플레이어는 약 5등신 일본 애니메이션 풍 캐릭터를 만들고, 체형 표현과
클래스를 선택한 뒤 **필드 입장**을 누른다. 작은 필드를 이동하고, 인벤토리를 열어
의상과 검을 교체하며, 검 공격 애니메이션을 실행한다.

이 문서는 의도적으로 **클라이언트 전용 프로토타입**만 다룬다. 네트워킹, 서버 권한,
로그인, 원격 데이터, 매치메이킹, 다른 플레이어, 적, 피해량, 퀘스트는 포함하지 않는다.

### 1.1 착수 당시 기준선과 완료 판정

2026-08-10 착수 당시 저장소에는 Vite/TypeScript/Three.js 실행 기반, 린트와 Vitest,
절차적 캐릭터 6종, 장비 5종, 필드 소품 4종, 개발용 에셋 갤러리가 있다. 이는
게임플레이 구현 완료를 뜻하지 않는다. `src/domain`, `src/game`, `src/render`, `src/ui`의
최종 모듈과 캐릭터 생성→필드→인벤토리→공격 흐름은 아직 구현하지 않았다.

현재 기준선은 **Phase 0의 잔여 정리부터 시작한다는 조건으로 개발 착수 가능**하다.
특히 다음 사항을 먼저 해소한 뒤 Phase 1로 넘어간다.

- 현재 `src/main.ts`의 에셋 갤러리는 개발 검수 화면이다. 최종 앱 로직을 그 파일에
  계속 추가하지 않고 `src/dev/AssetGallery.ts` 같은 별도 모듈로 옮긴다.
- `createAnimeAvatar`는 현재 클래스별 무기까지 붙이는 미리보기 팩토리다. 실제 게임용
  아바타는 몸/의상과 장비를 분리하고, 무기는 도메인 장비 상태를 읽는
  `EquipmentRenderer`만 `hand.R`에 부착하게 한다.
- 지팡이·활·화살통은 이번 마일스톤에서 갤러리/클래스 정체성용 시각 리소스다.
  필드 전투와 저장 카탈로그의 무기는 검 두 종류만 사용한다.
- `npm run lint`, `npm run test`, `npm run build`는 현재 통과한다. `test:e2e` 스크립트와
  Playwright 테스트는 Phase 9 전까지 자리표시자와 문서 상태로 유지할 수 있다.

2026-08-11에 Phase 7~9까지 구현·검증해 첫 로컬 플레이 가능 마일스톤을 완료했다. 최종
상태와 남은 시각/번들 한계는 9절 진행 로그와 `README.md`의 릴리스 정보에 기록한다.

## 2. 플레이어 관점의 사양

### 2.1 캐릭터 생성

생성 화면은 다음을 제공한다.

- 캐릭터 이름 입력: 2~16자, 로컬 표시용.
- 체형 표현: `feminine` 또는 `masculine`. 스타일화된 기본 몸, 얼굴/머리카락 프리셋,
  기본 팔레트를 바꾸지만 입력과 전투 로직은 바꾸지 않는다.
- 클래스: `warrior`, `mage`, `ranger`.
- idle 애니메이션 또는 idle 포즈가 보이는 실시간 3D 미리보기.
- 선택 클래스의 짧은 설명과 시작 장비.
- 이름과 클래스가 유효할 때만 활성화되는 **필드 입장** 버튼.

초기 클래스 차이는 표현과 시작 장비 차이로만 한정한다.

| 클래스 | 시작 의상 | 시작 무기 | 프로토타입에서의 역할 |
| --- | --- | --- | --- |
| 전사 (`warrior`) | 철제/파란 튜닉 | 연습용 검 | 검 교체를 검증하는 주 경로 |
| 마법사 (`mage`) | 남색 로브 | 연습용 검 | 지팡이는 생성 미리보기/갤러리에서만 사용 |
| 궁수 (`ranger`) | 녹색 여행복 | 연습용 검 | 활·화살통은 생성 미리보기/갤러리에서만 사용 |

하나의 무기 구현을 유지하면 세 개의 별도 전투 시스템을 피할 수 있다. 지팡이와 활
전투는 이 세로 기능 묶음 이후 동일한 장비 소켓 API 위에 추가한다.

### 2.2 필드

- 1 월드 단위를 1m로 취급하며 잔디, 길, 바위, 나무, 단순 충돌 장애물이 있는
  약 240 × 240m의 스타터 필드.
- 5등신 셀 셰이딩 아바타와 3인칭 추적 카메라.
- 필드 아바타는 약 1.7~2.4m, 기본 카메라는 세로 FOV 52°·거리 9.5m이며
  휠로 4.5~18m 범위를 조절한다.
- 카메라 방향 기준 이동: `WASD`, `Shift`로 달리기.
- 마우스 드래그로 카메라 회전. 초기에는 카메라 충돌이 필요 없다.
- HUD에는 캐릭터 이름, 클래스, 장착 검, 짧은 조작 안내를 표시한다.
- 적과 피해량 계산은 없다. 공격은 시각적 피드백만 제공한다.

### 2.3 인벤토리와 장비

`I` 키는 필드를 내리지 않고 인벤토리 오버레이를 열고 닫는다.

필수 슬롯은 다음과 같다.

| 슬롯 | 필수 콘텐츠 | 첫 세로 기능 묶음의 렌더링 방식 |
| --- | --- | --- |
| 무기 | 연습용 검, 달빛 검 | `hand.R`에 장착하는 독립 `THREE.Group` |
| 의상 | 시작 의상, 여행자 의상 | 팔레트/재질 및 모듈형 몸 변화 |
| 머리 | 없음, 시작 모자 | `head`에 장착하는 선택적 독립 그룹 |

사용자가 소유한 아이템을 선택하면 직렬화 가능한 장비 상태와 아바타의 외형이 즉시
업데이트된다. 아이템은 삭제·소비·네트워크 전송되지 않는다.

#### 필수 카탈로그 ID

구현 단계에서 이름이 흔들리지 않도록 첫 마일스톤의 ID를 다음과 같이 고정한다.

| 종류 | ID | 용도 |
| --- | --- | --- |
| 무기 | `weapon.training-sword` | 모든 클래스의 시작 검 |
| 무기 | `weapon.moon-sword` | 인벤토리 교체 검 |
| 의상 | `outfit.warrior-starter` | 전사 시작 외형 |
| 의상 | `outfit.mage-starter` | 마법사 시작 외형 |
| 의상 | `outfit.ranger-starter` | 궁수 시작 외형 |
| 의상 | `outfit.traveler` | 모든 클래스가 교체할 수 있는 공용 의상 |
| 머리 | `head.none` | 기본 비장착 상태 |
| 머리 | `head.starter-cap` | 머리 슬롯 부착 검증용 모자 |

`staff`, `bow`, `quiver` ID는 개발용 시각 에셋 카탈로그에는 존재할 수 있지만
`CharacterProfile.ownedItemIds`와 첫 마일스톤 장비 카탈로그에는 넣지 않는다.

### 2.4 공격

- 인벤토리가 닫혀 있을 때 마우스 왼쪽 버튼 또는 `F`로 `attack_1`을 시작한다.
- 공격은 설정 가능한 고정 시간 동안 지속하며, 짧은 입력 잠금 시간을 가진다.
- 장착한 검은 공격 중에도 계속 `hand.R`에 붙어 있어야 한다.
- 피격 판정, 피해량, 대상, 효과는 요구하지 않는다. 짧은 검기 효과는 검 휘두르기가
  효과 없이도 충분히 읽힌 뒤에만 추가한다.

## 3. 아키텍처 결정

### 3.1 기술 스택

| 관심사 | 선택 | 이유 |
| --- | --- | --- |
| 빌드/개발 서버 | Vite | 최소한의 TypeScript 브라우저 작업 흐름 |
| 언어 | TypeScript strict mode | 장비와 상태 계약을 명확히 표현 |
| 3D | Three.js | 브라우저 WebGL 씬, 애니메이션, 그룹 |
| UI | DOM + CSS | 메뉴 규모가 작아 UI 프레임워크가 불필요 |
| 테스트 | Vitest, 이후 Playwright E2E 스모크 테스트 | 순수 상태와 보이는 플레이 흐름을 각각 검증 |
| 저장 | `localStorage` | 백엔드 없이 로컬 영속성 충족 |

### 3.2 런타임 상태의 소유권

정본 상태는 직렬화 가능하게 유지하고 Three.js와 독립시킨다.

```ts
type BodyPresentation = "feminine" | "masculine";
type CharacterClass = "warrior" | "mage" | "ranger";
type EquipmentSlot = "weapon" | "outfit" | "head";
type AnimationState = "idle" | "run" | "sprint" | "attack_1";

interface CharacterProfile {
  version: 1;
  name: string;
  body: BodyPresentation;
  classId: CharacterClass;
  ownedItemIds: string[];
  equipped: Record<EquipmentSlot, string | null>;
}
```

`src/domain`은 이 상태를 검증하고 전이시킨다. `src/render`는 상태를 관찰해 Three.js
객체를 생성/제거하고, 활성 아이템 ID를 팩토리에 연결한다. 이 분리는 나중에 온라인
기능을 추가하더라도 UI와 에셋 모듈 전체를 다시 쓰지 않게 한다.

`src/assets/catalog.ts`의 현재 항목은 개발 갤러리가 팩토리를 찾기 위한 **시각 에셋
카탈로그**다. Phase 1의 `src/domain/catalog.ts`는 직렬화 가능한 아이템 정의와 시작
장비 규칙만 담는 **게임 정본 카탈로그**로 별도 작성한다. 도메인 파일은 `THREE.Group`,
재질, 팩토리 함수를 import하지 않는다. `EquipmentRenderer`가 두 카탈로그를 item ID로
연결한다.

### 3.3 아바타와 소켓 계약

현재 절차적 아바타를 다음 조건을 만족하는 게임용 모듈형 아바타로 정리한다.

- 머리 높이를 기준 단위로 삼는다.
- 전체 키는 약 다섯 머리 높이로 구성한다.
- 큰 머리, 작은 몸통, 짧은 팔다리, 읽기 쉬운 셀 셰이딩 색을 사용한다.
- 노드/소켓 이름은 정확히 `root`, `head`, `hand.R`, `hand.L`, `back`으로 한다.

초기 아바타는 단순한 팔다리 그룹 회전으로 움직일 수 있다. 이후 리깅된 GLB 아바타를
넣을 때는 어댑터를 통해 같은 부착점을 보존한다. 장비를 숫자 자식 인덱스나 광범위한
이름 탐색에 묶지 않는다.

게임용 아바타 팩토리는 장비를 자동 부착하지 않는다. 몸체 표현과 현재 의상 외형만
만들고, `AvatarRenderer`가 안정적인 노드/피벗을 소유하며 `EquipmentRenderer`가
도메인 상태에 따라 무기와 머리 장비를 교체한다. 클래스 콘셉트용 지팡이·활을 보여주는
기능이 필요하면 게임용 팩토리를 감싼 개발/생성 미리보기 어댑터에서만 부착한다.

강체 무기 팩토리 계약은 다음과 같다.

```ts
export interface EquipmentVisual {
  readonly itemId: string;
  readonly slot: "weapon" | "outfit" | "head";
  create(): THREE.Group;
}

// 무기 팩토리: 그립은 원점, 로컬 축을 문서화, 전역 씬 접근 금지.
```

나중에 생성한 무기는 `img2threejs`의 TypeScript 결과를 이 인터페이스 뒤로 어댑트한다.
해당 에셋은 게임 루프를 호출하거나 카메라를 조작하거나 장비 상태를 소유해서는 안 된다.

### 3.4 애니메이션 상태 우선순위

작고 명시적인 상태 선택기를 사용한다.

```text
attack_1 활성 상태       → attack_1
Shift + 달리기 임계값 이상 → sprint
이동 임계값 이상          → run
그 외                    → idle
```

`attack_1`은 설정된 종료 시점까지 우선한다. 공격 중에도 입력을 읽을 수는 있지만, 공격
포즈를 run으로 일찍 교체해서는 안 된다.

### 3.5 저장

- 캐릭터 확정 직후와 성공적인 장비 변경마다 저장한다.
- 앱 시작 시 `anime-field-rpg.profile.v1` 같은 하나의 키를 읽는다.
- 파싱한 데이터를 검증한다. 잘못되었거나 오래된 저장은 충돌 대신 캐릭터 생성으로
  안전하게 되돌린다.
- 테스트를 위해 생성 화면에 작은 **로컬 캐릭터 초기화** 동작을 제공한다.

## 4. 제안하는 파일 구조

```text
src/
  assets/
    avatar/createAnimeAvatar.ts
    equipment/createSword.ts
    equipment/createMageStaff.ts       # 개발/생성 미리보기 전용
    equipment/createRangerBow.ts       # 개발/생성 미리보기 전용
    field/createFieldProps.ts
    catalog.ts                         # 시각 에셋 카탈로그
  domain/
    character.ts
    catalog.ts
    equipment.ts
    save.ts
  game/
    GameApp.ts
    input.ts
    movement.ts
    collision.ts
    animationState.ts
  render/
    createRenderer.ts
    createField.ts
    ThirdPersonCamera.ts
    AvatarRenderer.ts
    EquipmentRenderer.ts
  ui/
    CreationScreen.ts
    InventoryPanel.ts
    Hud.ts
  dev/
    AssetGallery.ts                    # 게임 상태와 분리한 개발 검수 화면
  styles/
    global.css
  main.ts
tests/
  domain/equipment.test.ts
  domain/save.test.ts
  game/animationState.test.ts
  e2e/player-flow.spec.ts
```

파일은 각 단계에서 필요해질 때만 도입한다. 이 트리에 맞추기 위해 빈 폴더나 추측성
시스템을 미리 만들지 않는다.

## 5. 단계별 구현 계획

각 단계는 Codex CLI에 보내는 별도 요청 하나다. 다음 단계로 가기 전, 그 단계에서
명시한 검사를 완료한다.

### 모든 Phase에 적용하는 완료 프로토콜

각 Phase는 다음 조건을 모두 만족해야 완료로 표시한다.

1. 해당 Phase에 필요한 코드와 테스트만 변경하고 다음 Phase 기능을 선행 구현하지 않는다.
2. `npm run lint`, `npm run test`, `npm run build`를 모두 실행해 성공시킨다.
3. 개발 서버를 브라우저에서 열고 그 Phase의 수동 인수 항목을 직접 확인한다.
4. 시각 문제가 있으면 재현 절차와 수정 결과를 진행 로그에 기록한다.
5. 조작·명령·구조가 바뀌면 `README.md`를 갱신한다.
6. 위 증거가 없으면 코드가 존재하더라도 진행 표의 상태를 `완료`로 바꾸지 않는다.

### Phase 0 — 기존 스캐폴드 정리와 프로젝트 계약

**목표:** 이미 있는 에셋 갤러리 기준선을 보존하면서 최종 앱을 확장할 수 있는 작은
진입점과 명확한 에셋 경계를 만든다.

**구현 항목**

1. 기존 Vite, Three.js, TypeScript strict mode, ESLint, Vitest 설정과 에셋 테스트를
   기준선으로 사용하고 불필요하게 다시 초기화하지 않는다.
2. 현재 에셋 갤러리 조합 코드를 `src/dev/AssetGallery.ts`로 옮기고 `main.ts`는 앱 모드
   선택과 시작 함수 호출만 담당하게 한다.
3. 게임용 `createAnimeAvatar`가 무기를 자동 부착하지 않도록 몸/의상 생성과 개발용
   클래스 장비 미리보기를 분리한다. 기존 갤러리의 보이는 결과는 유지한다.
4. `dev`, `build`, `lint`, `test` 스크립트를 유지한다. `test:e2e`는 Phase 9에서 실제
   Playwright 설치와 함께 추가하며, 그 전에는 문서상 보류 상태로 둔다.
5. `AGENTS.md`, `README.md`, 이 계획 문서와 `docs_working/img2threejs` 제작 증거를
   보존한다.

**인수 검사**

- `npm run build`가 성공한다.
- `npm run lint`가 성공한다.
- 기존 9개 에셋 계약 테스트를 포함해 `npm run test`가 성공한다.
- 개발 서버에서 에셋 갤러리가 유지되며, 브라우저 콘솔 오류가 없다.
- `main.ts`에 갤러리 렌더링 세부 구현이 남아 있지 않다.
- 아바타를 만들었을 때 `hand.R`은 비어 있고, 별도 미리보기 어댑터에서만 클래스
  무기가 부착되는 테스트가 통과한다.

### Phase 1 — 도메인 모델과 로컬 저장

**목표:** 렌더링 전에 캐릭터·장비 상태를 신뢰할 수 있게 만든다.

**구현 항목**

1. 체형 표현, 클래스, 아이템 카탈로그, 장비 슬롯, 프로필 타입을 추가한다.
2. 세 클래스와 최소 두 종류의 검/의상 카탈로그 항목을 정의한다.
3. 기본 프로필 생성, 이름 검증, 소유 아이템 장착, 시작 장비 도출, 저장 직렬화/복원
   순수 함수를 만든다.
4. 비정상 저장 복구가 있는 `localStorage` 어댑터를 구현한다.

**테스트**

- 클래스 선택이 문서에 적힌 시작 장비를 부여한다.
- 소유하지 않은 아이템 장착은 거부된다.
- 검 장착은 무기 슬롯만 변경한다.
- 잘못된 저장 JSON 또는 지원하지 않는 버전은 안전하게 캐릭터 생성으로 시작한다.

### Phase 2 — 캐릭터 생성 화면

**목표:** 아직 3D 필드가 없어도 플레이어가 캐릭터를 선택하고 확정하게 한다.

**구현 항목**

1. 이름, 체형 표현 카드, 클래스 카드, 실시간 시작 장비 요약, 초기화 동작, 비활성/활성
   필드 입장 버튼이 있는 타이틀/생성 DOM 화면을 만든다.
2. 유효한 프로필을 확정하면 즉시 저장한다.
3. 유효 저장이 있을 때 생성 화면을 몰래 건너뛰지 말고 **계속하기**와 **새 캐릭터 생성**을
   제공한다.
4. `CreationScreen`은 확정된 직렬화 가능 프로필을 콜백으로 내보내지만 Three.js 객체를
   직접 만들지 않는다. Phase 2에서는 필드 대신 명시적인 준비 완료 상태를 보여주고,
   실제 화면 모드 전환은 Phase 3에서 연결한다.

**수동 인수 검사**

- 필드 입장 전 이름 검증이 보인다.
- 각 클래스를 선택하면 설명과 시작 장비가 바뀐다.
- 새로고침 후 마지막으로 확정한 프로필이 유지된다.

### Phase 3 — Three.js 필드와 3인칭 카메라

**목표:** UI에 숨은 게임플레이 의존성 없이 읽기 쉬운 스타일화된 필드를 렌더링한다.

**구현 항목**

1. 렌더러, 씬, 카메라, 크기 변경 처리, 시계, 애니메이션 루프를 설정한다.
2. 기존 `createFieldProps`의 나무·바위·풀·룬 이정표를 재사용하고, 잔디 평면, 길,
   방향 조명, 반구 조명, 안개, 하늘색으로 240 × 240m 셀 셰이딩 필드를 만든다.
3. 테스트 대상에 대해 안정적 오프셋을 유지하는 회전/추적 카메라를 구현한다.
4. 플레이어 이동 전에도 나무와 바위의 기본 충돌 볼륨을 추가한다.
5. 앱 모드를 `creation`과 `field`로 명시하고, Phase 2의 확정 프로필을 받아 필드 씬을
   시작한다. 모드 전환과 씬 수명주기는 `main.ts`가 아니라 작은 앱 조합 모듈이 맡는다.

**수동 인수 검사**

- 창 크기 변경 시 화면이 찌그러지거나 검은 캔버스가 남지 않는다.
- 카메라가 테스트 마커를 추적/회전하며 계속 바라본다.
- 목표 카메라 거리에서 필드가 읽기 쉬운 상태로 유지된다.

### Phase 4 — 임시 애니메 아바타와 이동

**목표:** 필드에 애니메 비율 캐릭터를 두고 움직인다.

**구현 항목**

1. 기존 `createAnimeAvatar`를 게임용 몸/의상 팩토리로 정리해 5등신 비율과 필수
   소켓을 가진 이름 있는 그룹을 만든다. 새 임시 아바타를 중복 작성하지 않는다.
2. 선택한 체형 표현, 클래스 시작 의상, `outfit.traveler` 외형을 렌더링할 수 있게 한다.
3. WASD 입력을 카메라 상대 이동으로 변환하고 `Shift`는 속도를 높인다.
4. 필드 경계와 단순 원/AABB 장애물에 대해 이동을 해결한다.
5. idle, run, sprint 절차적 애니메이션(팔다리 흔들기/몸통 상하 움직임)을 구현한다.

**수동 인수 검사**

- `WASD`가 월드 고정축이 아니라 화면 방향을 기준으로 움직인다.
- 아바타가 필드 밖으로 나가거나 최소 하나의 나무/바위를 통과할 수 없다.
- 회전과 달리기 중에도 카메라가 부드럽다.
- idle/run/sprint 전환이 튀지 않고 보인다.

### Phase 5 — 장비 카탈로그와 검 부착

**목표:** 향후 모든 아이템에 적용될 동일한 상태→렌더 경로로 검 교체를 보이게 한다.

**구현 항목**

1. 기존 `createTrainingSword`, `createMoonSword`를 독립 `THREE.Group` 팩토리 계약에
   맞게 재사용한다. 지팡이·활은 이 Phase의 게임 장비 카탈로그에서 제외한다.
2. 활성 무기 팩토리를 아바타의 정확한 `hand.R` 소켓에 부착한다.
3. 아바타 전체를 다시 만들지 않고 이전 외형을 dispose/제거한 후 새 외형을 추가하는
   `EquipmentRenderer`를 만든다.
4. `outfit` 슬롯에 따라 선명하게 구분되는 시작 의상·여행자 의상 팔레트/모듈형 메시
   변화를 구현한다.
5. 반복 교체 뒤에도 중복 무기나 끊어진 참조가 없도록 한다.
6. `head.none`과 `head.starter-cap`을 같은 상태→렌더 경로로 처리해 머리 슬롯 계약도
   검증한다.

**테스트와 수동 인수 검사**

- 순수 장비 테스트가 유효/무효 슬롯 변경을 다룬다.
- 검을 바꾸면 외곽선이 달라지고 이동 중에도 손 소켓 부착을 유지한다.
- 의상을 바꾸면 색/형태가 즉시 바뀐다.
- 반복 교체 후 씬에 유령 무기가 남지 않는다.

### Phase 6 — 인벤토리 UI

**목표:** 소유 장비를 사용하기 쉬운 게임 내 패널로 노출한다.

**구현 항목**

1. `I`로 열고 `I`/`Esc`로 닫는 HUD와 인벤토리 오버레이를 추가한다.
2. 아이템을 슬롯별로 그룹화하고 선택/장착 상태를 보이며, 사용할 수 없는 아이템은
   비활성화한다.
3. 클릭 처리에서 씬을 직접 바꾸지 않는다. 도메인 액션 → 저장 → 렌더 갱신 순으로
   선택을 처리한다.
4. 인벤토리가 열려 있는 동안 이동 입력은 멈추되, 뒤의 필드는 계속 렌더링한다.

**수동 인수 검사**

- 인벤토리를 열어도 아바타/카메라 위치가 초기화되지 않는다.
- 마우스 클릭으로 두 검과 두 의상을 모두 장착할 수 있다.
- Escape는 패널을 닫고 이동 조작을 되돌린다.
- 새로고침 후 장착 아이템 ID와 보이는 상태가 유지된다.

### Phase 7 — 검 공격 애니메이션

**목표:** 전투 시스템을 추가하지 않고도 반응성이 좋고 읽기 쉬운 공격을 만든다.

**구현 항목**

1. `attack_1` 상태, 시작 제한, 쿨다운, 입력 매핑을 추가한다.
2. 장착 검이 의도적인 가로/대각선 베기를 그리도록 몸통, 오른팔/소켓, 선택적 루트
   전진 움직임을 애니메이션한다.
3. `setTimeout`이 아닌 delta time 기반의 애니메이션 타임라인을 사용한다.
4. 공격 포즈가 run 애니메이션에 덮어써지지 않도록 공격 우선순위를 보장한다.
5. 공격이 끝난 뒤 현재 이동 입력에 따라 idle/run/sprint로 돌아간다.

**테스트와 수동 인수 검사**

- `animationState` 테스트가 공격 우선과 이동 애니메이션 복귀를 증명한다.
- 인벤토리가 닫혀 있을 때만 마우스 왼쪽 버튼/F가 공격을 실행하며, 열려 있을 때는
  실행하지 않는다.
- 두 검 모두를 시험한다. 전체 휘두르기 동안 `hand.R` 정렬을 유지해야 한다.
- 공격 중 `W`를 누르고 있으면 휘두르기가 끝난 뒤에만 run으로 돌아온다.

### Phase 8 — 시각 다듬기와 img2threejs 에셋 어댑터

**목표:** 세로 기능 묶음을 불안정하게 만들지 않고 표현을 개선한다.

**구현 항목**

1. 5등신 실루엣에 맞게 셀 셰이딩, 성능이 허용되면 외곽선, 팔레트, 안개, 카메라
   프레이밍을 조정한다.
2. 공격을 더 명확히 만들 때만 작은 검기 효과를 추가한다.
3. 기존 `img2threejs` 제작 명세와 리뷰 기록을 런타임 팩토리 계약에 연결하는 문서화된
   어댑터 메모를 정리한다.
4. 연습용 검과 달빛 검의 그립 원점, 로컬 축, dispose, 소켓 정렬을 최종 검토한다.
   세 번째 게임 무기는 이번 마일스톤의 완료 조건이 아니며 별도 요청 없이 추가하지 않는다.
5. 개발용 지팡이·활·화살통과 필드 소품은 보존하되 전투/도메인 범위를 확장하지 않는다.

**수동 인수 검사**

- 캐릭터, 의상, 검이 필드를 배경으로도 명확히 보인다.
- 생성/추가한 검에 전역 상태 부작용이 없고 정상적으로 교체된다.
- `npm run build` 뒤 브라우저 콘솔에 누락 에셋 요청이 없다.

### Phase 9 — 종단 간 검증과 인수인계

**목표:** 프로토타입을 반복 시연할 수 있고 다음 기능에 안전하게 넘길 수 있게 한다.

**구현 항목**

1. Phase 9에서만 Playwright를 개발 의존성으로 추가하고 `test:e2e` 스크립트와 브라우저
   스모크 테스트를 만든다: 전사 생성 → 필드 입장 → 이동 → 인벤토리 열기
   → 달빛 검 장착 → 공격.
2. 조작이 바뀌었다면 `README.md`에 선택적 수동 체크리스트를 추가한다.
3. 사용하지 않는 시작 파일과 개발용 콘솔 출력을 제거한다.
4. 최종 패키지 버전과 알려진 한계를 기록한다.

**릴리스 인수 검사**

1. 로컬 저장을 지운다.
2. 원하는 체형 표현으로 이름 있는 전사를 만든다.
3. 필드에 입장해 이동, 달리기, 카메라 회전, 장애물 충돌을 확인한다.
4. 인벤토리에서 달빛 검과 여행자 의상을 장착한다.
5. 정지 상태와 달리기 상태에서 공격한다.
6. 새로고침하고 계속하기를 선택한다. 이름, 클래스, 의상, 검이 맞는지 확인한다.
7. `npm run lint`, `npm run test`, `npm run build`를 모두 성공시킨다.

### Phase 11 — MMORPG NPC·플레이어 아이템 리소스 라이브러리

**목표:** 기존 5등신 캐릭터·소켓·장비 축 계약을 유지하면서 외부 에셋 없이 대량의
원본 절차형 시각 리소스를 제공한다. 게임 도메인 범위를 넓히지 않고 개발용 갤러리에서
검색·검수 가능한 교체형 라이브러리로 둔다.

**구현 항목**

1. 24개 NPC archetype과 3개 세력을 조합한 NPC 72종을 추가한다. 각 역할은 두 개 이상의
   이름 있는 시그니처 부품을 가지며 `hand.R`, `hand.L`, `head`, `back` 소켓을 유지한다.
2. 무기 72종, 복장·갑옷 48종, 머리 장비 24종, 방패·등·보조장비 24종으로 플레이어
   아이템 168종을 추가한다. 무기는 그립 원점 `(0, 0, 0)`, `+Y` 날/공격 축, `+Z` 앞축을
   공통 계약으로 사용한다.
3. 모든 정의는 고유 ID·한글 라벨·카테고리·태그·독립 팩토리를 제공한다. 생성 객체는
   원본 절차형 출처, 품질, 부착, deep-dispose 메타데이터와 이름 있는 부품을 가진다.
4. 개발용 갤러리를 캐릭터·NPC·아이템·필드 탭과 이름/ID/태그 검색으로 확장하고,
   착용 장비에는 마네킹형 문맥 미리보기를 제공한다.

**완료 조건**

- [x] NPC 72종과 플레이어 아이템 168종의 ID가 중복되지 않는다.
- [x] 240개 팩토리를 전수 생성해 유한하고 비어 있지 않은 bounds를 확인한다.
- [x] NPC 소켓·역할 시그니처 부품과 무기 그립·축·착용 부위 anchor 계약을 테스트한다.
- [x] 갤러리 탭 수량, 검색, NPC·갑옷·무기 WebGL 렌더와 콘솔·페이지·네트워크 오류를
  Playwright E2E로 확인한다.
- [x] `npm run lint`, 79개 단위 테스트, `npm run build`, E2E 2개를 통과한다.

## 6. 에셋 제작 경로

### 첫 플레이 가능 빌드

필수 시각 요소는 현재 만들어진 절차적 캐릭터·장비·필드 소품을 재사용한다. 에셋을
다시 만드는 작업으로 이동·인벤토리 구현을 막지 않는다. 다만 현재 클래스 미리보기용
장비 자동 부착은 Phase 0에서 게임용 팩토리와 분리한다.

### 개선 경로

1. 첫 마일스톤 이후에만 프리미티브 아바타를 직접 제작했거나 적절히 라이선스가 확인된
   리깅 GLB로 교체하는 작업을 검토한다.
2. 실제 스켈레톤 본 이름을 `head`, `hand.R`, `hand.L`, `back` 소켓으로 매핑하는
   `AvatarAdapter`를 만든다.
3. `GLTFLoader`로 idle/run/attack 클립을 불러오고 동일한 애니메이션 상태 기계에
   매핑한다.
4. 모든 메시가 동일한 아마추어와 호환 가능한 스킨 웨이트를 사용할 때만 의상 팔레트를
   별도 스키닝 메시로 교체한다.
5. `img2threejs`의 캐릭터 분석 결과는 비율·실루엣·팔레트 근거로 사용하고, 강체 장비
   결과는 독립 팩토리로 사용한다. 참조/제작 메모는 `docs_working/img2threejs`에 보존하며
   런타임 코드에는 원본 참조 이미지를 import하지 않는다.

## 7. 비목표와 보류한 결정

다음 항목은 잊은 것이 아니라 의도적으로 보류한다.

- 네트워크 프로토콜, 서버 권한, 동기화, 부정행위 방지, 인증, 영구 계정, 데이터베이스,
  매치메이킹, 채팅.
- 적, 타기팅, 피격 상자, 피해 수치, 전리품, 퀘스트, 지도, 사운드, 고비용 파티클 효과,
  버전 1 이후의 저장 마이그레이션.
- 최종 캐릭터 리그, 블렌드셰이프, 천 시뮬레이션, 세부 얼굴 커스터마이징, 고급 렌더러/
  포스트 프로세싱, 모바일 지원, 접근성 키 재매핑.
- 클래스별 지팡이/활 전투. 카탈로그는 미래 무기 유형을 허용하지만, 지금은 장비
  파이프라인을 증명하기 위해 검만 필요하다.

## 8. 위험과 통제 방법

| 위험 | 통제 방법 |
| --- | --- |
| 최종 캐릭터 아트가 게임플레이를 지연 | 소켓 호환 프리미티브 아바타부터 시작 |
| 생성 무기의 방향이 잘못됨 | 그립 원점 팩토리 계약을 강제하고 테스트 씬에서 검토 |
| 움직이는 팔다리와 의상이 겹침 | 팔레트 교체부터 시작하고, 공유 리그일 때만 스키닝 의상 추가 |
| UI와 렌더러 상태 불일치 | 도메인 상태를 정본으로 두고 UI는 액션 호출, 렌더러는 상태 관찰 |
| 범위가 MMO 작업으로 팽창 | 이 마일스톤에 모든 네트워크/서버 코드를 넣지 않음 |
| 시각 작업 이후 회귀 | 매 단계 린트·단위 테스트·빌드·브라우저 인수 검사 실행 |

## 9. 진행 로그

실제로 작업을 완료한 뒤에만 이 표를 갱신한다.

| Phase | 상태 | 날짜 | 메모 |
| --- | --- | --- | --- |
| 0 | 완료 | 2026-08-10 | 갤러리 진입점과 클래스 장비 미리보기를 분리하고 lint·12개 테스트·build·브라우저 검증 완료 |
| 1 | 완료 | 2026-08-10 | 프로필·게임 카탈로그·장비 전이·버전 저장 복구 구현, lint·25개 테스트·build·브라우저 회귀 검증 완료 |
| 2 | 완료 | 2026-08-10 | 생성·저장 프로필 선택·준비 완료 DOM 화면 구현, lint·25개 테스트·build·브라우저 수동 인수 검증 완료 |
| 3 | 완료 | 2026-08-11 | 초기 80×80 필드를 240×240m로 확장하고 MMO형 아바타 배율·FOV·줌·이동형 그림자와 외곽 소품까지 브라우저 재검증 완료 |
| 4 | 완료 | 2026-08-10 | 5등신 아바타·카메라 기준 이동·달리기·경계/소품 충돌·idle/run/sprint 구현, lint·38개 테스트·build·브라우저 검증 완료 |
| 5 | 완료 | 2026-08-10 | 검·의상·머리 장비 팩토리와 소켓 기반 EquipmentRenderer 구현, lint·42개 테스트·build·브라우저 검증 완료 |
| 6 | 완료 | 2026-08-10 | 슬롯별 인벤토리·즉시 장착/저장·이동 입력 차단 구현, lint·46개 테스트·build·브라우저 검증 완료 |
| 7 | 완료 | 2026-08-11 | delta time 기반 attack_1·쿨다운·초기 이동 잠금·이동 상태 복귀·검기 구현, 두 검과 인벤토리 차단 브라우저 검증 완료 |
| 8 | 완료 | 2026-08-11 | 이동형 보조광·톤/대비·검기 가독성 조정, 검 좌표/폐기 계약과 img2threejs 런타임 어댑터 문서화 완료. 캐릭터 미술 점수 0.69 한계 유지 |
| 9 | 완료 | 2026-08-11 | Playwright 전사 생성→이동→장비 교체→공격→저장 복원 E2E 추가, lint·57개 테스트·build·E2E·브라우저 검증 완료 |
| 10 | 부분 완료 | 2026-08-19 | HUD 분리, GLB 로딩/fallback·의상/모자 호환, 범용 본 별칭·선택적 클립 어댑터, 번들 분할 완료. 미술 점수 0.69 보정은 img2threejs 하드 스톱과 로컬 도구 부재로 차단 |
| 11 | 완료 | 2026-08-19 | 원본 절차형 NPC 72종과 플레이어 아이템 168종, 검색 가능한 255종 갤러리, 전수 계약 테스트와 E2E 검증 완료 |

### 캐릭터 선택·생성 UI 개선 기록

- 2026-08-10: 시작 흐름을 MMO형 `selection → creation → selection → field` 씬 전환으로 재구성했다.
- 저장 캐릭터가 있으면 중앙 3D 전신 미리보기와 선택 상태를 표시하고, 필드 입장·생성·2단계 삭제 명령을 제공한다.
- 생성 씬에서 체형과 클래스를 고를 때 실제 아바타·장비 렌더러를 재사용한 중앙 미리보기가 즉시 갱신되며, 생성 완료 후 선택 로비로 복귀한다.
- 필드 HUD를 캐릭터 프레임·자원 바·미니맵·단축 슬롯 형태로 개편했다. lint·46개 테스트·build와 빈 슬롯부터 삭제까지의 브라우저 수동 검증을 완료했다.

### Phase 3 브라우저 검증 기록

- 재현 절차: 저장 캐릭터로 필드에 진입한 뒤 브라우저 뷰포트를 900×650에서
  1050×720으로 변경했다. 초기 구현에서는 캔버스 CSS 크기만 먼저 바뀌고 렌더 버퍼가
  이전 크기에 머물거나 다음 애니메이션 프레임 전까지 지형이 보이지 않았다.
- 수정 결과: `ResizeObserver`, 창 `resize` 이벤트, 렌더 루프의 크기 변화 감지를 함께
  사용하고 실제 크기가 달라질 때만 버퍼와 카메라 투영 행렬을 갱신한 뒤 즉시 한 프레임을
  렌더하도록 했다. 두 크기에서 CSS/버퍼 크기가 각각 일치하고 지형·표식·HUD가 유지됐으며
  브라우저 콘솔 경고와 오류가 없음을 확인했다.

### Phase 4 브라우저 검증 기록

- 저장된 여성형 전사 프로필로 필드에 진입해 고정 표식이 클래스 시작 의상을 입은 5등신
  아바타로 교체되고, 이름 있는 팔·다리 피벗과 빈 `hand.R` 소켓이 유지됨을 확인했다.
- 캔버스를 포커스한 뒤 이동 입력을 반복해 카메라 방향을 기준으로 아바타 위치와 진행
  방향이 함께 바뀌고, HUD가 `RUN`으로 전환되며 팔·다리가 교차하는 보행 포즈가 보임을
  확인했다. 마우스 드래그 뒤에도 카메라는 아바타를 중심으로 부드럽게 추적했다.
- 필드 경계, 나무 원 충돌체, 바위 AABB 충돌체의 정지·축 미끄러짐은 순수 이동 테스트로
  고정했으며 브라우저 콘솔 경고와 오류는 없었다.

### Phase 5 브라우저 검증 기록

- 저장된 여성형 전사로 필드에 진입해 연습용 검이 정확한 `hand.R` 소켓에 부착되고,
  걷기 자세에서 오른팔과 함께 움직이는 것을 확인했다.
- 검을 달빛 검으로 교체하고 의상을 전사 튜닉과 여행자 의상 사이에서 반복 전환해
  이전 시각 객체가 남지 않고 선택한 외형만 보이는 것을 확인했다.
- 초보자 모자와 `head.none`을 전환해 `head` 소켓 장착과 제거를 확인했으며 브라우저
  콘솔 경고와 오류는 없었다.

### Phase 6 브라우저 검증 기록

- `I`와 HUD 버튼으로 인벤토리를 열고 `I`·`Escape`로 닫았으며, 슬롯별 소유·장착·사용
  불가 상태와 HUD의 장착 검 이름이 선택 즉시 갱신됨을 확인했다.
- 인벤토리가 열린 동안 `W` 입력을 반복해도 플레이어 좌표가 바뀌지 않았고, 닫은 뒤
  입력이 다시 동작하는 구조를 확인했다.
- 달빛 검·여행자 의상·초보자 모자를 장착한 뒤 새로고침하고 계속하기를 선택해 세 장비가
  모두 복원됨을 확인했으며 브라우저 콘솔 경고와 오류는 없었다.

### Phase 7 브라우저 검증 기록

- 여성형 전사의 연습용 검과 달빛 검으로 각각 `F` 공격을 실행해 공격 요청이 시작되고,
  두 무기가 `hand.R`에 붙은 상태로 유지되는 것을 확인했다.
- 인벤토리가 열린 동안 `F`를 눌러도 공격 횟수가 증가하지 않았고, 닫은 뒤에는 다시
  실행됐다. Playwright에서는 `W`를 누른 공격이 `attack_1`을 유지한 뒤 run으로 복귀했다.
- 공격은 0.72초 delta time 타임라인, 0.16초 초기 이동 잠금, 0.28초 쿨다운을 사용한다.
  setTimeout 기반 전투 상태 전환은 없으며 브라우저 콘솔 경고와 오류도 없었다.

### Phase 8 브라우저 검증 기록

- 필드에 이동형 보조광, 노출·화면 가장자리 대비 조정을 적용해 캐릭터와 검이 밝은 초원
  배경에서도 구분되는 것을 정면 필드 캡처에서 확인했다.
- 연습용 검에서 달빛 검으로 교체한 뒤 금색 가드·보라색 그립과 HUD 이름이 즉시 바뀌고,
  이전 검이 남지 않는 것을 확인했다. 두 팩토리의 그립 원점·`+Y` 칼끝·`+Z` 앞면과 deep
  dispose 계약을 테스트로 고정했다.
- 기존 img2threejs blockout 루프는 3/3에서 중단된 상태이고 여성 전사 시각 점수는 0.69로
  승인 기준 0.70 미만이다. 이번 단계에서는 형상을 추가 보정하지 않고 런타임 어댑터 경계와
  알려진 GLB 한계를 문서화했다.

### Phase 9 종단 간 검증 기록

- Playwright Chromium에서 로컬 저장 삭제, 남성형 전사 생성, 필드 이동, 달빛 검·여행자
  의상 장착, 이동 중 공격, 새로고침 복원을 하나의 스모크 테스트로 통과했다.
- E2E는 콘솔 오류, 페이지 예외, 실패 네트워크 요청도 실패 조건으로 검사한다. `npm run lint`,
  57개 단위 테스트, 프로덕션 빌드와 E2E 1개가 통과했다.
- 프로덕션 빌드에는 600 kB 초과 단일 청크 경고 1건이 남아 있으며, 패키지 버전은
  `0.1.0`으로 기록했다.

### Phase 10 후속 안정화 기록

- 2026-08-19: 필드 HUD DOM과 상태 표시를 `src/ui/Hud.ts`로 분리해 `GameApp`이 게임 루프와
  조합만 담당하도록 정리했다.
- 여성 전사 GLB가 준비되기 전 아바타를 숨기고 로드 또는 canonical 리그 검증 실패 시 절차형
  fallback을 복구한다. 전용 스키닝 여행자 의상과 GLB 머리 소켓용 모자 정렬을 적용했다.
- `AvatarAdapter`에 외부 본 이름 별칭과 선택적 idle/run/sprint/attack GLTF 클립 매핑을 추가했다.
  현재 GLB의 `animationCount`는 0이므로 실제 런타임은 절차 애니메이션을 유지한다.
- 앱 모드와 Three.js 코어/애드온을 분리해 최대 프로덕션 청크를 586.61 kB로 낮췄으며 기존
  600 kB 경고를 해소했다. lint, 71개 단위 테스트, build와 여성 전사 GLB 경로 E2E 1개가 통과했다.
- 인앱 브라우저 수동 검증은 관리자 보안 정책 확인 실패로 실행하지 못했다. Playwright Chromium
  E2E는 여성 전사 생성, GLB 요청, 여행자 의상·초보자 모자·달빛 검 장착, 공격과 복원을 통과했다.
- 여성 전사 형상은 시각 점수 0.69 상태다. 기존 img2threejs blockout 상태가
  `max-correction-loops-reached:blockout:3/3` 하드 스톱이며 현 실행 환경에 Python과 Blender가 없어,
  새 승인 단계와 실행 환경이 제공되기 전에는 얼굴·앞머리·골반/무릎 수동 보정을 진행하지 않는다.

### Phase 11 대규모 리소스 라이브러리 기록

- 2026-08-19: NPC 24 archetype × 3 세력으로 상인·대장장이·경비·귀족·농부·광부·선원·
  학자·성직자·연금술사·음유시인·치유사·사냥꾼·길드 접수원·요리사·어부·마구간지기·
  사서·수도사·유목민·북방 전사·엘프 장인·드워프 기술자·수인 정찰병 72종을 추가했다.
- 플레이어 장비는 18개 무기 archetype과 4개 테마로 72종, 복장·갑옷 48종, 머리 장비
  24종, 방패·망토·화살통·마도서·등불 등 보조장비 24종으로 총 168종을 추가했다.
- 전수 팩토리 생성, bounds, ID, 역할 부품, 소켓·그립·축·anchor, 독립 폐기 계약을
  8개 전용 테스트로 고정했다. 전체 lint, 79개 단위 테스트, 프로덕션 build가 통과했다.
- Playwright E2E 2개가 기존 생성→필드→장비→공격→복원 흐름과 새 갤러리 240종 검색·
  대표 WebGL 렌더·콘솔/페이지/네트워크 무오류를 통과했다. 연결 브라우저의 로컬 주소
  접근은 관리자 정책 확인 실패로 차단되어 자동 캡처 3장을 직접 검토했다.

### 라이브 MMORPG 참고 필드 스케일 조정 기록

- 2026-08-11: Guild Wars 2의 FOV·카메라 위치·줌 감도·캐릭터 키 보정 설계와 FINAL
  FANTASY XIV의 휠 줌·3인칭 각도 조절 방식을 비교했다. 공개되지 않은 엔진 내부 수치를
  임의로 복제하지 않고 공식적으로 확인 가능한 조절 원칙만 반영했다.
- Guild Wars 2 API의 Queensdale 좌표 예시와 1좌표=2ft 환산을 참고하면 공개 월드 존은
  km급이다. 이 프로토타입은 콘텐츠 밀도가 낮으므로 240×240m 스타터 필드로 제한하고,
  중심에서 경계까지 스프린트 약 17초를 목표로 했다.
- 필드 아바타는 원본 5등신 메시의 0.36배인 약 1.7~2.4m, 기본 카메라는 세로 FOV 52°,
  추적 높이 1.25m, 거리 9.5m, 줌 4.5~18m로 고정했다. 생성/선택 미리보기 배율은 유지했다.
- 외곽까지 나무·바위·이정표·풀과 충돌체를 결정적으로 재배치하고 안개를 88~215m로
  늘렸다. 태양과 그림자 카메라는 플레이어를 따라가도록 바꿨으며 브라우저 기본/원거리/
  근거리 줌에서 콘솔 경고·오류 없이 지형과 캐릭터가 판독됨을 확인했다.
- `npm run lint`, 57개 단위 테스트, 프로덕션 빌드, Playwright E2E 1개와 `git diff --check`를
  통과했다. 프로덕션 빌드에는 기존 600 kB 초과 단일 청크 경고 1건이 남아 있다.

### 선행 에셋 준비 기록

- 2026-08-10: `docs_working/reference/concepts`의 6개 턴어라운드를 바탕으로
  `img2threejs` 엄격 명세를 작성·검증했다. 5등신 여성형/남성형 × 전사/마법사/레인저
  아바타, 검·지팡이·활·화살통, 나무·바위·풀·룬 이정표 팩토리와 브라우저 에셋
  갤러리를 추가했다. 이는 Phase 8 전체 완료가 아니라 이후 게임 단계에서 사용할
  소켓 호환 시각 기반의 선행 준비다.

### img2threejs 콘셉트 실루엣 패스 기록

- 2026-08-10: 6개 턴어라운드를 다시 비교해 공통 아바타를 약 5.5등신으로 조정하고,
  클래스·체형 조합별 헤어를 곡선 기반 테이퍼 리본 지오메트리로 교체했다. 여성 전사의
  긴 포니테일, 여성 마법사의 웨이브와 땋은 머리, 여성 레인저의 단발과 사이드 브레이드,
  남성형의 스파이크·로우 포니테일을 별도 이름 파트로 구성했다.
- 전사의 단일 견갑·가슴 스트랩·분할 튜닉, 마법사의 후드 망토·분할 로브, 레인저의
  후드·짧은 코트·화살통 실루엣을 구현했다. 생성 화면에서는 전사 검, 마법사 지팡이,
  레인저 활과 화살통을 대표 장비로 보여주되 필드의 공유 검 전투 규칙은 바꾸지 않았다.
- `root`, `head`, `hand.R`, `hand.L`, `back`, 의상 마운트와 사지 애니메이션 노드는
  유지했다. 브라우저 정면·생성 화면 검수에서 콘솔 경고와 오류가 없었으며 비교 시트의
  자체 시각 점수는 0.68이다. 얼굴 턱선, 손가락, 의상 주름·재봉선은 후속 세부 패스다.

### 여성 전사 hero v2 집중 보정 기록

- 2026-08-10: 사용자 비교 이미지에서 지적된 여성 전사의 비인간적인 얼굴과 직선형 실루엣을 우선 보정했다.
- 여성 전사에만 연속 턱선 얼굴, 피팅 몸통, 굵어진 팔다리, 짧은 소매·장갑, 다발형 고포니테일, 흰 테두리 분할 튜닉과 축소한 단일 견갑을 적용했다. 다른 다섯 프리셋의 경로는 유지했다.
- 브라우저 정면·3/4·회전 렌더와 비교 시트를 검토했다. 의상·포니테일 식별성은 개선됐으나 관절 분절과 얼굴 토폴로지는 아직 콘셉트 수준에 미달해 시각 점수 0.59와 `refine-code` 판정을 기록했다.
- lint, 53개 테스트, 프로덕션 빌드와 브라우저 콘솔 무오류를 확인했다. 다음 고품질 패스는 연속 스키닝 메시 또는 DCC 기반 GLB 교체가 핵심이다.

### 여성 전사 Blender GLB 교체 기록

- 2026-08-10: Blender 5.2 헤드리스 생성 스크립트로 여성 전사의 연속 신체 메시, 18개 관절 아마추어,
  얼굴·고포니테일·전사 튜닉·단일 견갑·벨트·장갑·부츠를 구성하고 GLB와 편집 가능한 `.blend`를 생성했다.
- `GLTFLoader` 기반 비동기 교체 어댑터를 추가했다. 여성형 전사만 GLB를 사용하고 다른 프리셋과 로딩 전
  화면은 기존 절차형 아바타를 유지한다. 기존 장비를 새 `hand.R` 소켓으로 옮기고 의상 가시성을 동기화한다.
- Blender 휴지 회전을 보존하는 상대 애니메이션 오프셋으로 런타임을 수정했다. 선택 화면과 필드에서 팔이
  뒤집히지 않으며 검이 손에서 아래 방향으로 붙고, 앞·옆·뒤 회전에서도 체적이 유지됨을 확인했다.
- GLB 검사 결과는 85개 노드, 61개 메시, 17개 재질, 1개 스킨, 18개 관절이며 경고가 없다. 비교 검수
  점수는 0.64로 승인 기준 0.70 미만이다. 얼굴·손·머리카락의 컨셉 수준 품질은 수동 Blender 스컬프와
  리토폴로지가 필요한 후속 미술 작업으로 남긴다.
- 이후 장비 제작에도 `img2threejs`의 상세 명세, 재질, 그립 원점·소켓 방향, 다각도 비교 게이트를 필수로
  적용하고 단순 프리미티브 조합을 완료품으로 판정하지 않는다.
- 2026-08-10 얼굴 집중 보정에서는 두상을 64×48 세그먼트의 별도 스키닝 메시로 분리하고 턱선을 좁혔다.
  구형 안구를 얕은 아몬드형 눈판으로 교체하고 상안검·축소한 홍채/동공을 추가했으며, 앞머리의 과도한
  그림자를 런타임에서 제외해 얼굴 가독성을 높였다. GLB는 88개 노드, 64개 메시, 17개 재질, 1개 스킨,
  18개 관절로 갱신됐다.
- 브라우저 정면·측면·후면 재검수 점수는 0.66으로 이전 0.64보다 개선됐지만 승인 기준 0.70 미만이다.
  막대형 앞머리, 손가락 없는 손, 원통형 팔다리가 전체 품질을 제한하므로 이번 결과를 완료품으로 판정하지
  않는다. 자동 픽셀 진단은 게임 화면 배경과 컨셉 시트 구도 차이를 전경으로 오인해 참고값으로만 기록했다.
- 2026-08-10 해부학 v3에서는 손바닥·네 손가락·엄지를 별도 연속 메시로 만들고 `hand.L`/`hand.R`에
  스키닝했다. 어깨·팔꿈치·손목과 엉덩이·무릎·종아리·발목에 서로 다른 단면 반경을 사용해 원통형 팔다리와
  구슬형 관절 인상을 줄였으며, 부츠 실루엣도 넓혀 종아리와 연결했다.
- 갱신한 GLB는 90개 노드, 66개 메시, 17개 재질, 1개 스킨, 18개 관절이며 검사 경고가 없다. 갤러리
  정면·측면·후면과 실제 필드에서 손가락 판독성, 체적 유지, 검 소켓 장착을 확인했다. 비교 검수 점수는
  0.69로 이전 0.66보다 향상됐으나 승인 기준 0.70에는 못 미쳤다. 얼굴과 머리카락은 이번 단일 해부학
  보정 그룹에서 제외했으며 다음 집중 보정 대상으로 남긴다.
- 자동 픽셀 진단의 IoU 0.299는 UI 전체 화면과 컨셉 시트의 배경·프레이밍을 모델 실루엣으로 오인해
  정량 품질값으로 채택하지 않았다. `img2threejs` blockout 보정 루프는 3/3 상한에 도달해 상태를
  정지했으며, 다음 얼굴·머리카락 보정은 새 승인 단계로 시작해야 한다.
- 2026-08-10 런타임 재현 결과, 여성 전사 선택 화면은 절차형 아바타를 즉시 표시한 뒤 비동기 GLB 로딩
  완료 시 장면 그래프를 교체해 한 프레임 이상 외형이 튀는 문제가 있다. 저장 프로필은 손상되지 않았으나
  이전 인벤토리 검수에서 선택한 `outfit.traveler`와 `head.starter-cap`이 복원되면서 전사 튜닉을 숨기고
  단순한 호환 시각물을 표시해 최종 캐릭터가 구형처럼 보인다. 수정 시 GLB 준비 전 캐릭터를 노출하지 않는
  로딩 상태와, GLB 전용 의상·머리 장비의 품질/호환 정책을 함께 정해야 한다.

## 10. 다음 구현 요청의 시작점

Phase 7~9와 첫 로컬 플레이 가능 마일스톤은 완료됐다. Phase 10의 런타임·구조·번들 후속 작업과
Phase 11의 NPC·플레이어 아이템 240종 라이브러리도 완료됐으며 남은 항목은 여성 전사 미술 승인
기준 0.70 통과다. 캐릭터 미술을 계속할 경우 중단된
img2threejs blockout 상태를 우회하지 않고 새 승인 단계를 연 뒤 Python과 Blender 실행 환경에서
얼굴·앞머리·골반/무릎을 한 보정 그룹씩 검증한다.
