# 애니메 필드 RPG 프로토타입 — 에이전트 작업 지침

## 목표

스타일화된 판타지 RPG의 **로컬 싱글플레이 브라우저 프로토타입**을 만든다.
플레이어는 약 5등신의 일본 애니메이션 풍 캐릭터를 생성하고, 체형 표현과
클래스를 선택한 뒤 3인칭 필드에 입장한다. 필드에서 이동하고, 인벤토리에서
장비를 교체하고, 근접 공격 애니메이션을 실행할 수 있어야 한다.

정식 요구 사항과 구현 순서는 [`docs/IMPLEMENTATION_PLAN.md`](docs/IMPLEMENTATION_PLAN.md)를
우선으로 한다. 행동을 바꾸기 전에 반드시 이 문서를 읽는다. `README.md`는
개발자를 위한 설치·실행·조작 안내서다.

## 범위 경계

- 이번 마일스톤은 **클라이언트 전용·오프라인**이다. 네트워킹, 계정, 백엔드,
  인증, 매치메이킹, 클라우드 저장소, 멀티플레이어 상태를 추가하지 않는다.
- 현재 로컬 프로토타입 저장만 `localStorage`에 보관한다.
- 데모를 범용 게임 엔진으로 키우거나 대형 Claude-of-Duty 코드베이스를 복사하지
  않는다. 코드는 작고 명시적이며 교체하기 쉽게 유지한다.
- 필수 세로 기능 묶음은 캐릭터 생성 → 필드 입장 → 이동 → 인벤토리/장비 교체 →
  공격 애니메이션이다.
- 적 전투, 피해량, 퀘스트, 상점, 절차적 월드 생성은 이후 요청으로 명시되지 않는
  한 범위 밖이다.

## 기술 기준선

- Vite, TypeScript strict mode, Three.js를 사용한다.
- 메뉴와 HUD는 순수 TypeScript, DOM, CSS로 구현한다. 이 프로토타입에 React 등
  UI 프레임워크를 추가하지 않는다.
- 게임 상태는 프레임워크와 독립적으로 둔다. Three.js 객체는 상태를 렌더링할 뿐,
  캐릭터·인벤토리·장비의 정본 상태를 소유하지 않는다.
- 데스크톱 브라우저(키보드·마우스)를 우선 대상으로 한다. 모바일 조작은 필요 없다.
- 라이선스가 확인된 GLB를 의도적으로 추가하기 전까지는 직접 만든 프리미티브와
  원본 임시 아트를 우선한다. 확인되지 않은 제3자 모델·텍스처를 커밋하지 않는다.

## 필수 설계 규칙

- 플레이어 아바타는 안정적인 스켈레톤/노드 명명 규칙을 하나만 가져야 한다.
  장비는 추측성 탐색이 아니라 이름 있는 소켓(`hand.R`, `head`, `back`)에 부착한다.
- 무기는 독립적인 `THREE.Group` 팩토리다. 그립 원점은 `(0, 0, 0)`이며 로컬
  앞/위 축을 팩토리에 문서화한다.
- 의상은 먼저 슬롯 데이터로 표현한다. 초기 시각 구현은 재질·팔레트 교체로
  충분하며, 동일한 아바타 스켈레톤을 공유할 때만 스키닝된 의상 메시를 추가한다.
- 클래스 선택은 초기 팔레트, 의상, 시작 장비에 영향을 준다. 클래스마다 별도의
  이동 또는 장비 시스템을 만들지 않는다.
- 공격 상태는 일시적으로 이동 애니메이션보다 우선한다. 공격 구간이 끝날 때까지
  이동 입력이 run 애니메이션을 재시작시키면 안 된다.

## 아키텍처와 파일 책임

구현 계획에 명시된 모듈 경계를 사용한다. 특히 다음을 지킨다.

- `src/domain/`은 직렬화 가능한 타입, 카탈로그 데이터, 순수 상태 전이를 담는다.
- `src/game/`은 게임 루프, 입력 해석, 이동, 충돌, 애니메이션 상태 선택을 담당한다.
- `src/render/`는 Three.js 씬 설정, 아바타 렌더링, 카메라, 필드, 장비 부착
  어댑터를 담당한다.
- `src/ui/`는 DOM 화면과 패널을 담당한다. UI는 도메인 액션을 호출하지만 Three.js
  객체를 직접 수정하지 않는다.
- `src/assets/`는 원본 에셋 팩토리와 에셋 메타데이터를 담는다. 에셋 팩토리에
  게임플레이 판단을 넣지 않는다.

`main.ts`를 거대하게 만들지 않는다. 이 파일은 모듈 조합과 앱 시작만 담당한다.

## 품질 게이트

각 단계가 끝날 때 가장 좁은 범위의 관련 검사를 실행하고, 결과를 구현 계획의
진행 로그에 기록한다.

1. `npm run lint`
2. `npm run test`
3. `npm run build`
4. 브라우저에서 앱을 실행하고 해당 단계의 수동 인수 검사를 수행한다.

TypeScript 컴파일만으로 시각 동작이 맞다고 주장하지 않는다. 반드시 브라우저에서
확인한다. 시각 문제가 발견되면 고치기 전에 정확한 재현 절차를 진행 로그에 남긴다.

## 작업 방식

- 한 번에 하나의 일관된 구현 단계만 변경하고, 각 변경 뒤 앱이 실행 가능하도록
  유지한다.
- 의존성을 추가하기 전, 브라우저와 현 의존성만으로 부족한 이유를 설명한다.
  의존성 추가는 최소화한다.
- 사용자의 변경을 보존한다. 기존 파일을 수정하기 전 작업 트리를 조사하며, 현
  요청과 관계없는 작업을 reset하거나 폐기하지 않는다.
- 조작, 명령, 프로젝트 구조가 바뀌면 `README.md`를 갱신한다.
- 실제 완료, 새 제약 발견, 명시적으로 합의한 결정 변경 때만
  `docs/IMPLEMENTATION_PLAN.md`를 갱신한다. 범위를 몰래 바꾸지 않는다.

## 이번 마일스톤의 완료 조건

사용자가 소스 코드를 수정하지 않고 다음을 할 수 있을 때만 프로토타입을 완료로
판정한다.

1. 캐릭터 생성 화면에서 체형 표현과 정의된 판타지 클래스 중 하나를 선택한다.
2. 미리보기를 확인하고 **필드 입장**을 누른다.
3. 3인칭 추적 카메라로 보이는 필드를 이동한다.
4. 인벤토리를 열어 최소 두 종류의 검과 두 종류의 의상을 장착하고, 선택한 모습이
   아바타에 즉시 반영되는 것을 확인한다.
5. 검 공격을 실행해 장착한 검이 아바타와 함께 움직이는 공격 애니메이션을 본다.
6. 페이지를 새로고침해도 `localStorage`에서 선택한 캐릭터와 장비가 복원된다.
